--- a/index.html
+++ b/index.html
@@ -145,8 +145,16 @@
             text-align: center;
             cursor: pointer;
             transition: all 0.3s;
+            border: none;
+            background: none;
+            color: inherit;
+            font-size: inherit;
             border-bottom: 3px solid transparent;
         }
+        .tab:focus-visible {
+            outline: 2px solid #00ff9d;
+            outline-offset: -2px;
+        }

         .tab.active {
             background: rgba(0, 168, 255, 0.2);
@@ -415,14 +423,14 @@
             </div>
         </div>

-        <div class="tabs">
-            <div class="tab active" data-tab="networks">WiFi Networks</div>
-            <div class="tab" data-tab="devices">Devices</div>
-            <div class="tab" data-tab="analysis">Analysis</div>
-            <div class="tab" data-tab="settings">Settings</div>
+        <div class="tabs" role="tablist">
+            <button class="tab active" data-tab="networks" role="tab" aria-selected="true" aria-controls="networks" id="tab-networks">WiFi Networks</button>
+            <button class="tab" data-tab="devices" role="tab" aria-selected="false" aria-controls="devices" id="tab-devices" tabindex="-1">Devices</button>
+            <button class="tab" data-tab="analysis" role="tab" aria-selected="false" aria-controls="analysis" id="tab-analysis" tabindex="-1">Analysis</button>
+            <button class="tab" data-tab="settings" role="tab" aria-selected="false" aria-controls="settings" id="tab-settings" tabindex="-1">Settings</button>
         </div>

-        <div class="tab-content active" id="networks">
+        <div class="tab-content active" id="networks" role="tabpanel" aria-labelledby="tab-networks">
             <div class="network-list" id="networkList">
                 <p style="text-align: center; padding: 20px; color: #aaa;">
                     No networks detected yet. Click "Start Scan" to begin.
@@ -430,24 +438,24 @@
             </div>
         </div>

-        <div class="tab-content" id="devices">
+        <div class="tab-content" id="devices" role="tabpanel" aria-labelledby="tab-devices" hidden>
             <div class="device-list" id="deviceList">
                 <p style="text-align: center; padding: 20px; color: #aaa;">
                     No devices discovered yet.
                 </p>
             </div>
         </div>

-        <div class="tab-content" id="analysis">
+        <div class="tab-content" id="analysis" role="tabpanel" aria-labelledby="tab-analysis" hidden>
             <div class="stat-card" style="text-align: left; margin-bottom: 10px;">
                 <h3 style="color: #74b9ff; margin-bottom: 10px;">📊 Network Analysis</h3>
                 <div id="analysisContent">
                     <p>Start scanning to see detailed analysis...</p>
                 </div>
             </div>
         </div>

-        <div class="tab-content" id="settings">
+        <div class="tab-content" id="settings" role="tabpanel" aria-labelledby="tab-settings" hidden>
             <div class="stat-card" style="text-align: left;">
                 <h3 style="color: #74b9ff; margin-bottom: 10px;">⚙️ Settings</h3>

@@ -570,11 +578,16 @@
             saveSettingsBtn.addEventListener('click', saveSettings);

             // Tab switching
-            tabs.forEach(tab => {
+            tabs.forEach((tab, index) => {
                 tab.addEventListener('click', () => {
                     const tabId = tab.getAttribute('data-tab');
                     switchTab(tabId);
                 });
+                tab.addEventListener('keydown', (e) => {
+                    const dir = e.key === 'ArrowRight' ? 1 : e.key === 'ArrowLeft' ? -1 : 0;
+                    if (dir) {
+                        const next = tabs[(index + dir + tabs.length) % tabs.length];
+                        next.focus(); switchTab(next.getAttribute('data-tab'));
+                    }
+                });
             });

             // Settings slider
@@ -674,10 +687,13 @@
         }

         function switchTab(tabId) {
-            tabs.forEach(t => t.classList.remove('active'));
-            tabContents.forEach(c => c.classList.remove('active'));
-            document.querySelector().classList.add('active');
-            document.getElementById(tabId).classList.add('active');
+            tabs.forEach(t => {
+                const active = t.getAttribute('data-tab') === tabId;
+                t.classList.toggle('active', active);
+                t.setAttribute('aria-selected', active);
+                t.setAttribute('tabindex', active ? '0' : '-1');
+            });
+            tabContents.forEach(c => { c.classList.toggle('active', c.id === tabId); c.hidden = c.id !== tabId; });
         }

         async function startContinuousScan() {
