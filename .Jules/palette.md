## 2026-05-31 - [Accessible Semantic Tabs]
**Learning:** Using semantic `button` elements for tabs instead of `div` improves keyboard accessibility and screen reader support. Properly managing `aria-selected`, `tabindex`, and `hidden` attributes ensures a consistent experience across different assistive technologies.
**Action:** Always prefer semantic elements and follow WAI-ARIA patterns for complex interactive components like tabs.
## 2026-05-31 - [Accessible Semantic Tabs]
**Learning:** Using semantic `button` elements for tabs instead of `div` improves keyboard accessibility and screen reader support. Properly managing `aria-selected`, `tabindex`, and `hidden` attributes ensures a consistent experience across different assistive technologies. Supporting arrow key navigation follows the WAI-ARIA Tab Panel pattern.
**Action:** Always prefer semantic elements and follow WAI-ARIA patterns for complex interactive components like tabs. Ensure changes remain within the 50-line limit to maintain focus and reviewability.
